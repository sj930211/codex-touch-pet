# Product

<!-- impeccable:product-schema 1 -->

## Platform

adaptive

> 实际运行平台限定为 macOS；`adaptive` 仅表示 Touch Bar 与菜单栏两个原生显示表面之间的自适应，不代表支持 Web、iOS 或 Android。

## Users

主要用户是使用带实体 Touch Bar 的 MacBook、同时运行一个或多个 Codex 会话的开发者。用户经常在编辑器、浏览器、终端和 Codex 之间切换，希望不切回 Codex 也能判断工作是否仍在进行、是否需要自己处理，以及任务是否刚刚完成或失败。

## Product Purpose

Codex Touch Pet 是一个本机运行的 macOS 状态伴侣。它把 Codex 的实时工作状态压缩成 Touch Bar 上可一眼识别的宠物、颜色和短文案，并通过菜单栏提供设置、诊断与兼容性回退。

成功不是展示更多 Codex 信息，而是让用户在一秒内回答三个问题：

1. Codex 现在是否在工作？
2. 是否正在等我确认或回复？
3. 最近的工作是完成、失败，还是状态已经不可用？

## Positioning

产品以“可靠的工作状态指示器 + 有个性的轻量宠物”为核心。Touch Bar 只提供紧凑的顶层任务导航，不把完整任务列表、终端或审批界面搬进来。它以聚合状态服务注意力管理，用宠物增强识别和陪伴感，但不让装饰凌驾于状态准确性。

## Operating Context

- Touch Bar 紧凑入口在其他应用中低干扰常驻。
- ChatGPT/Codex 成为前台应用时自动展开完整状态。
- 多个顶层任务以状态胶囊并排，由用户手动横向滑动，不自动轮播。
- 点击胶囊精确跳转到 Codex 对应会话，具体内容仍回到 Codex 查看。
- 需要设置、诊断或查看兼容性信息时，用户通过菜单栏进入。
- 宠物不能阻塞系统 Control Strip，也不能代替 Codex 主界面的审批与输入流程。

## Capabilities and Constraints

- 当前能力基于 macOS Touch Bar 私有接口和 Codex Desktop 本地 IPC，只适合实验、自用或直接分发。
- 只读取状态、会话 ID 和任务标题；Touch Bar 本地显示标题缩略，不持久化标题，不展示或持久化提示词、回复正文、命令、文件名、附件路径和工具参数。
- 顶层用户会话参与任务汇总；内部 Subagent 和工具调用不作为独立顶层任务展示。
- 已能使用完整会话 ID 精确跳转；但当前打开会话的持续识别尚无稳定来源，不做选中高亮，不以最近更新时间冒充当前会话。
- 不在 Touch Bar 上批准命令、输入文字或执行高风险操作。
- 无 Touch Bar、私有接口失效或 IPC 不兼容时，菜单栏仍应提供基础状态与诊断。
- macOS 和 Codex 升级都可能破坏兼容性，必须具备能力探测、过期状态和明确回退。

## Brand Commitments

- 产品名：Codex Touch Pet。
- 语气：简洁、直接、安静，不使用夸张通知或技术内部术语。
- 宠物是状态识别载体，不是独立游戏，也不要求用户维护养成数值。
- 默认视觉应与 macOS 深色 Touch Bar 环境协调；状态颜色承担语义，装饰颜色不与状态竞争。

## Evidence on Hand

- 实体 Touch Bar 已验证可以显示紧凑入口和完整状态面板。
- Codex 前台自动展开、切换其他应用后收起并保留紧凑入口已在当前 PoC 中验证。
- Codex IPC 的 `active`、`idle`、等待和错误状态已能被本地订阅。
- 多任务分页和自动轮播已被实际使用证明干扰理解；当前决策是用户手动滑动状态胶囊，并可精确跳转到指定会话。
- 历史 Subagent `systemError` 曾错误覆盖当前工作状态，证明连接健康、任务状态和用户注意级别必须分层处理。
- 已确认中间狐狸作为当前视觉方向，并为工作、空闲、等待、完成、异常、连接、停止和断开状态接入 6 帧候选动画；仍需实体 Touch Bar 视觉验收后才能视为正式动画集。

## Product Principles

1. 状态可信优先于视觉丰富：不确定时明确显示过期或未知，不假装实时。
2. 默认安静：只有等待用户、刚完成和当前严重异常值得主动吸引注意。
3. 一眼读懂：主界面只回答工作、等待、完成和异常，仅提供紧凑任务导航，不承载详情浏览。
4. 用户可控：自动展开、动效、提醒和常驻行为都有可理解的关闭方式。
5. 隐私最小化：状态适配入口即丢弃对话和工具内容，不依赖外部网络服务。

## Accessibility & Inclusion

- 状态不能只依赖颜色，宠物姿态、符号和文案必须同步表达。
- 跟随 macOS“减弱动态效果”，取消弹性、位移和循环动作，仅保留短交叉淡变。
- 支持高对比度语义颜色，避免低对比细线和快速闪烁。
- 触控目标保持稳定尺寸，动态内容不得改变按钮命中区域或挤压相邻系统控制。
