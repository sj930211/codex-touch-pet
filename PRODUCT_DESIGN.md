# Codex Touch Pet 产品设计 v0.1

## 1. 产品定义

Codex Touch Pet 是一个仅在本机运行的 macOS 小工具：在 Touch Bar 中用一只低干扰的动态宠物表达 Codex 当前状态，并在需要用户处理时提供明显但克制的提醒。

核心价值不是把完整任务界面搬到 Touch Bar，而是让用户不用切回 Codex，也能回答三个问题：

1. Codex 现在是否在工作？
2. 是否正在等我确认或补充信息？
3. 刚才的任务是否完成或出错？

## 2. 产品原则

- 默认安静：空闲时只占一个紧凑区域，不长期遮挡系统控制。
- 状态优先：宠物表情服务于状态识别，不做无意义的持续动画。
- 可打断：用户触摸、切换应用或系统需要控制条时，动画和展开状态立即让位。
- 本机隐私：不显示、上传或持久化提示词、回复正文和工具输出。
- 随时可退：接口不可用或进程异常时，立即恢复系统 Control Strip。
- 尊重减少动态：开启“减弱动态效果”后，只使用颜色、图标和一次性淡入淡出。

## 3. 推荐体验模式

采用“常驻小宠物 + Codex 前台时完整展示”，不永久占满其他应用的 Touch Bar。

### 紧凑态

Touch Bar 系统区域保留一个约 44–60pt 的宠物入口：

```text
[ ʕ•ᴥ•ʔ ]                         [系统控制]
```

- 空闲时缓慢眨眼，每 4–8 秒一次。
- 工作时使用轻微的两帧呼吸或敲键盘动作。
- 点击后激活 Codex 并展开当前任务状态。

### 展开态

```text
[ ʕ◕ᴥ◕ʔ ] [ 正在处理 2 项工作 ]
```

- ChatGPT/Codex 成为前台应用时自动展开。
- 切换到其他应用时收起完整面板，Control Strip 小宠物继续常驻。
- 多个活动任务只显示汇总数量，例如“正在处理 2 项工作”，不显示 1/2、2/2 之类的分页编号。
- 任务完成、停止或失败后，汇总数量随状态实时更新。

## 4. 状态与宠物行为

| Codex 状态 | 文案 | 宠物表现 | 颜色 | 自动行为 |
|---|---|---|---|---|
| 未连接 | Codex 未连接 | 睡觉 | 中性灰 | 保持紧凑 |
| `notLoaded` / 未知 | 状态暂不可用 | 探头观察 | 次级蓝灰 | 不打扰 |
| `idle` | Codex 空闲 | 缓慢眨眼 | 青蓝 | 紧凑 |
| `active` | Codex 正在工作 | 敲键盘/思考 | 蓝色 | 紧凑，可点击展开 |
| `waitingOnApproval` | 等待你的确认 | 举手并轻点一次 | 琥珀色 | 常驻入口提醒；Codex 前台时完整展示 |
| `waitingOnUserInput` | 等待你的回复 | 问号气泡 | 琥珀色 | 常驻入口提醒；Codex 前台时完整展示 |
| turn `completed` | 已完成 | 一次短庆祝 | 绿色 | 展开 2–3 秒后收起 |
| `systemError` | 运行异常 | 晕圈/停住 | 红色 | 常驻入口提醒；Codex 前台时完整展示 |

状态优先级：错误 > 等待用户 > 完成瞬时态 > 工作中 > 空闲 > 未连接。

多个任务同时存在时，只显示汇总数量和等待处理数量，不展示正文、命令、工具参数、文件名或分页编号。这样用户能直接知道当前有几项工作，不会被 1/2、2/2 的循环打扰。

## 5. 交互设计

- 点击宠物：激活 Codex，并切换紧凑态与展开态。
- 再次打开或切回 ChatGPT/Codex：自动展开完整状态。
- 点击状态文案：激活 Codex Desktop；首版不依赖未验证的深链跳转。
- 长按宠物：进入 30 分钟安静模式，只保留状态颜色，不自动展开。
- 点击“收起”：立刻返回系统控制条和紧凑宠物。
- 错误或等待状态被处理后：平滑回到工作中或空闲，不突然闪烁。

不提供“在 Touch Bar 上批准命令”的功能。审批仍在 Codex 主界面完成，避免误触和权限语义模糊。

## 6. 动效规范

- 空闲：2–4 帧，低频眨眼；不连续高频播放。
- 工作：4–6 fps 的短循环，动作幅度不超过宠物高度的 10%。
- 完成：总时长不超过 1.2 秒，只播放一次。
- 等待：首次进入时轻微弹性提示一次，随后静止。
- 状态切换：120–180ms 淡变；允许任何新状态立即中断旧动画。
- 减少动态模式：取消位移和弹性，只保留 150ms 交叉淡变。

为降低 OLED 长时间固定显示风险，紧凑宠物可在不影响触控区域的范围内偶尔做 1–2pt 的位置调整。

## 7. 技术架构

```text
Codex Desktop IPC ──> Status Adapter ──> Pet State Machine ──> Touch Bar Renderer
       │                    │                    │                    │
       └─ 版本探测          ├─ 多任务聚合        ├─ 优先级/瞬时态      ├─ 私有全局模式
                            └─ 隐私过滤          └─ 减少动态          └─ 公开前台降级

独立 app-server / notify ───────────────────────────────> 兼容与完成兜底
```

### macOS 应用

- Swift/AppKit 菜单栏应用作为正式宿主。
- 使用一个很薄的 Objective-C 动态调用层封装 Touch Bar 私有 selector。
- 不在编译期链接 DFR 私有 framework；启动时只做 selector 能力探测。
- 菜单栏负责设置、诊断、退出和无 Touch Bar 设备的状态回退。

### 状态适配器

主路径：

1. 连接 `~/.codex/ipc/ipc.sock`。
2. 通过长度前缀 JSON 帧注册只读客户端。
3. 从线程清单取得候选线程 ID。
4. 使用 owner discovery 找到活动线程所有者。
5. 订阅定向 snapshot/patch，仅提取 `threadRuntimeStatus`、turn status、等待标记和未读完成标记。
6. 断开前发送取消 following，避免留下订阅状态。

兼容路径：

- 独立 app-server 用于发现最近线程和读取静态元数据，但不把 `notLoaded` 当作实时状态。
- 用户明确同意后，可把现有 Codex `notify` 配置改为 fan-out helper，作为完成事件冗余通道；首版不自动修改配置。
- IPC 版本不兼容时退化为“Codex 进程在线/离线 + 最近完成通知”，并在菜单栏显示兼容性提示。

## 8. 线程选择与多任务

选择顺序：

1. 正在等待用户处理的线程。
2. 当前活跃窗口对应的线程。
3. 仍为 `active` 的最近线程。
4. 最近完成但尚未展示庆祝的线程。
5. 最近使用的 `idle` 线程。

Touch Bar 紧凑态和完整面板都优先呈现聚合后的最重要状态；多个任务时显示处理数量和等待数量，不展示线程正文或标识信息。

## 9. 隐私与安全

- 只解析状态字段；正文、附件路径、命令内容和工具参数在适配器入口即丢弃。
- 默认不写状态日志。诊断模式只记录时间、协议版本、状态枚举和错误码。
- 不需要 OpenAI API Key，也不发起外部网络请求。
- 不修改 SIP、不注入系统进程、不替换系统 Touch Bar 文件。
- 不自动执行审批、输入或 Codex 控制操作。

## 10. 稳定性与恢复

- 启动时探测四个必要 selector；缺失任意一个就禁用全局 Touch Bar 模式。
- `SIGINT`、`SIGTERM`、正常退出和崩溃恢复路径都必须撤销 tray item 和 modal bar。
- 每次展开设置最长持续时间；只有等待用户和错误状态允许保持。
- 发现 TouchBarServer/ControlStrip 不健康时只停止自身并提示重启电脑，不自动循环重启系统服务。
- IPC 断线使用指数退避重连，并在 30 秒内保持最后已知状态的“过期”标记。
- macOS 或 Codex 升级后首次启动自动执行兼容性自检，不直接展示全局 bar。

## 11. 分发策略

- 目标渠道：开发者签名、notarization、直接下载或自用安装。
- 不以 Mac App Store 为首要渠道，因为核心体验依赖私有 API。
- 首次启动明确说明“实验性 Touch Bar 集成”，并提供一键禁用全局模式。
- 没有 Touch Bar 或兼容性失败时，应用仍可作为菜单栏 Codex 状态宠物运行。

## 12. 版本路线

### v0.1：可日用 MVP

- 手动启动菜单栏应用。
- 单个当前线程的 `idle / active / completed`。
- 紧凑宠物、手动展开、自动清理。
- IPC 和 Touch Bar 版本探测。
- 不修改 Codex 配置，不开机启动。

### v0.2：状态完整

- `waitingOnApproval / waitingOnUserInput / systemError`。
- 多线程聚合。
- 减少动态、安静模式、主题选择。
- 用户可选的 completion notify 冗余通道。

### v1.0：可分发

- 签名与 notarization。
- 升级兼容性自检。
- 崩溃恢复和诊断包。
- 菜单栏完整回退体验。

## 13. v0.1 验收标准

- 实体 Touch Bar 能稳定显示宠物至少 2 小时，不阻塞正常系统控制。
- 能在 1 秒内反映 `idle ↔ active`。
- 完成状态只庆祝一次，不重复提醒。
- 强制结束进程后，Control Strip 能自动恢复。
- Codex Desktop 未运行、升级不兼容或 IPC 断开时不崩溃。
- 默认运行期间不产生包含对话正文的日志或本地文件。

## 14. 推荐产品决策

首版采用“系统区域常驻小宠物，Codex 前台时完整展示”的方案。它在其他应用中只保留窄入口，在用户回到 Codex 时提供完整状态和工作数量汇总，同时避免永久占用整条 Touch Bar。
