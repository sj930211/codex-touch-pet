# Codex Touch Pet 产品设计方案 v0.2

> 状态：核心状态伴侣、狐狸动画和紧凑任务导航已进入实现与真机验收阶段。
>
> 产品方向：安静、可靠的 Codex 工作伴侣，而不是 Touch Bar 任务管理器。

## 1. 设计目标

Codex Touch Pet 要让用户不切回 Codex，也能在一秒内判断：

1. Codex 是否正在工作；
2. 是否有工作等待自己确认或回复；
3. 最近是否完成、失败，或者状态已经不可用。

宠物负责提升状态辨识度和陪伴感；状态准确性、低干扰和系统兼容性始终优先。

## 2. 本方案的关键决策

| 议题 | 推荐决策 | 原因 |
|---|---|---|
| 产品形态 | Touch Bar 状态伴侣 + 菜单栏控制中心 | Touch Bar 适合快速感知，菜单栏适合设置和诊断 |
| 默认展示 | 其他应用显示窄宠物，Codex 前台显示完整面板 | 保留常驻感，不长期占用整条 Touch Bar |
| 多任务 | 并排状态胶囊，手动横向滑动，不自动轮播 | 保留空间效率，同时让用户主动选择要查看的任务 |
| 状态表达 | 宠物姿态 + 文案 + 语义颜色 | 避免只靠颜色或只靠表情造成误读 |
| 错误策略 | 红色仅表示当前有效的严重错误 | 历史失败和内部任务错误不能覆盖当前工作状态 |
| 任务层级 | 用户顶层会话参与汇总，Subagent 折叠到父任务 | 内部执行细节不是用户眼中的独立工作 |
| 会话导航 | 按钮绑定完整会话 ID 精确跳转；暂不高亮当前会话 | 跳转目标可验证，但当前 UI 焦点仍没有稳定可信的外部来源 |
| 宠物风格 | 推荐统一的小型像素宠物素材 | 比 Unicode 字符更稳定，也更容易形成清晰动画状态 |
| 直接操作 | Touch Bar 不提供审批、输入和任务控制 | 避免误触和权限语义不清 |
| 养成玩法 | 首版不做饥饿值、等级和奖励系统 | 避免让状态工具变成需要维护的负担 |

## 3. 范围

### 3.1 本阶段包含

- 紧凑宠物入口；
- Codex 前台完整状态面板；
- 工作、等待、完成、失败、系统异常、未连接和状态过期；
- 多任务状态胶囊、手动横向滑动和指定会话精确跳转；
- 顶层会话与内部 Subagent 分层；
- 菜单栏设置、诊断和兼容性提示；
- 安静模式、减少动态和无 Touch Bar 回退；
- 完成、失败、断线和恢复的一次性事件反馈。

### 3.2 明确延期

- 当前打开会话的精确识别和选中高亮；
- 宠物皮肤商店、养成、等级和统计排行；
- 云端同步、多人协作状态和移动端联动；
- Touch Bar 内审批、终止任务或输入回复。

延期能力可以预留数据接口，但不能影响首版文案、布局和验收。

## 4. 信息架构

产品由三个界面层级组成：

```text
Touch Bar 紧凑态
    | 点击 / Codex 前台
    v
Touch Bar 完整态  <---->  Codex 主应用
    |
    | 设置 / 诊断
    v
菜单栏控制中心
```

### 4.1 紧凑态

紧凑态只保留一个固定尺寸宠物入口，不显示长文案：

```text
[ 宠物 ]                                  [系统控制]
```

- 在其他应用中常驻；
- 点击后激活 Codex 并展开完整面板；
- 触控尺寸不随表情、徽标或动画变化；
- 普通工作不闪烁，等待或异常只在首次进入状态时提示一次；
- 状态未知时显示中性姿态，不显示红色错误。

### 4.2 完整态

完整态由固定宠物、固定主状态、可滑动任务胶囊和固定连接状态组成：

```text
[ 宠物 ]  工作中  [●任务A] [!任务B] [✓任务C] …  ● 在线
```

主状态和连接状态永远占据稳定位置。中间区域仅由用户手动横向滑动，不自动轮播、不显示页码和左右箭头。标题仅作显示缩略，点击后使用隐藏的完整会话 ID 跳转，不以标题匹配会话。

### 4.3 菜单栏控制中心

菜单栏不复制 Touch Bar，而是承担解释、设置和诊断：

```text
Codex Touch Pet
状态              正在处理 2 项工作
需要处理          1 项
最后更新          刚刚
连接              正常
Touch Bar         兼容
----------------------
自动展开          [开关]
安静模式          [开关]
减少动态          跟随系统
----------------------
重新连接
诊断信息
退出
```

首层菜单不展示线程 ID、IPC 版本和内部错误码；这些信息只进入“诊断信息”。

## 5. 视觉方向

### 5.1 设计语言：Quiet Companion

- 黑色 Touch Bar 背景保持系统原生感；
- 主要状态用系统字体和稳定的高对比文字表达；
- 宠物是唯一有性格的视觉焦点；
- 不使用大面积发光、渐变、持续闪烁或装饰性粒子；
- 宠物动画幅度克制，状态变化比循环动画更重要。

### 5.2 宠物素材

推荐从字符表情升级为原创像素宠物：

- 统一画布和视觉重心，所有帧尺寸一致；
- 单色主体 + 状态色点缀，适应深色背景；
- 每种持续状态最多 2 至 4 帧；
- 完成事件可使用 4 至 6 帧的一次性动画；
- 不使用 emoji，避免系统字体和版本造成外观漂移；
- 保留字符宠物作为资源加载失败时的回退。

首套动作建议：

| 状态 | 姿态 | 动作 |
|---|---|---|
| 空闲 | 坐着观察 | 低频眨眼 |
| 工作 | 面向小键盘 | 轻微敲击或呼吸 |
| 等待确认 | 举手 | 进入时轻点一次，随后静止 |
| 等待回复 | 看向用户 | 问号徽标，随后静止 |
| 完成 | 抬头或举爪 | 播放一次短庆祝 |
| 任务失败 | 低头停住 | 不循环抖动 |
| 系统异常 | 警觉并停止动作 | 红色警示徽标 |
| 未连接 | 睡眠 | 静止或极低频呼吸 |
| 状态过期 | 探头观察 | 灰色时钟徽标 |

### 5.3 语义颜色

| 语义 | 建议色系 | 使用边界 |
|---|---|---|
| 工作 | 青蓝 | 工作中和正常连接，不表达错误 |
| 等待用户 | 琥珀 | 需要确认或回复 |
| 完成 | 绿色 | 只作为短暂完成反馈 |
| 中性 | 灰色 | 空闲、停止、未连接、状态未知 |
| 严重错误 | 红色 | 当前有效的系统异常或仍有效的顶层任务严重失败 |

颜色只能增强状态，不能替代文案、姿态和符号。红色不用于普通工作、历史错误或内部 Subagent 失败。

### 5.4 文字

- 使用 macOS 系统字体；
- 主状态采用中等或半粗字重；
- 数量和更新时间作为次级信息；
- 不使用负字距，不使用全大写英文标签；
- 文案必须能在单行容器中稳定显示，超长信息不进入 Touch Bar。

## 6. 状态模型

单一枚举无法可靠表达“连接正常但某个任务失败”等组合。本方案把状态拆成三个维度。

### 6.1 连接健康度

```text
connected      已连接且状态新鲜
stale          已连接或曾连接，但状态超过新鲜窗口
disconnected   IPC 已断开
incompatible   协议或 Touch Bar 能力不兼容
systemError    当前系统级异常
```

### 6.2 工作状态

```text
idle
working
waitingApproval
waitingInput
completed
interrupted
failed
```

### 6.3 用户注意级别

```text
none       不需要用户处理
notice     一次性告知，例如完成或恢复连接
action     等待确认或等待回复
critical   当前严重错误，需要查看
```

三个维度分别计算，最终再映射到显示状态。这样一个历史任务失败不会把当前工作状态直接改成红色系统异常。

## 7. 状态解析规则

### 7.1 任务层级

任务分为：

1. 顶层用户会话：计入“正在处理 N 项工作”；
2. 内部 Subagent：归属父任务，不单独计数；
3. 工具调用：只作为内部活动，不成为主状态；
4. 无法识别来源的线程：进入诊断，不主动提升为严重错误。

### 7.2 展示优先级

推荐优先级不是简单的错误枚举排序，而是：

1. 当前有效、状态新鲜的系统级严重异常；
2. 需要用户确认或回复的顶层任务；
3. 正在工作的顶层任务汇总；
4. 刚发生且尚未展示过的完成或失败事件；
5. 空闲；
6. 未连接、过期或未知。

历史错误保留在诊断记录中，不覆盖正在进行的工作。只有没有任何当前工作、等待或新鲜状态时，未解决的顶层任务失败才进入主展示。

### 7.3 新鲜度

每个状态都带最后更新时间：

- 状态在合理窗口内更新：正常展示；
- IPC 断线但仍在短暂宽限期：保持最后状态，并标记“正在重新连接”；
- 超过宽限期：显示“状态可能已过期”；
- 恢复连接：播放一次恢复事件，不重复播放历史完成动画。

具体时间窗口在技术设计阶段根据 IPC 心跳和实际延迟确定，产品文案不绑定未经验证的秒数。

## 8. 多任务体验

### 8.1 固定主状态

| 条件 | 主文案 |
|---|---|
| 1 项工作中 | 工作中 |
| 多项工作中 | 工作中 |
| 工作中且有等待 | 工作中 |
| 只有待确认 | 待确认 |
| 只有待回复 | 待回复 |
| 无活动任务 | 空闲 |

### 8.2 任务胶囊规则

- 只显示顶层用户任务，不让内部 Subagent 增加胶囊数量；
- 显示经过长度限制的任务缩略名，完整标题仅用于本地辅助功能，不进入诊断或日志；
- 使用胶囊背景色、细边框和文字标签表达任务状态，不重复叠加状态图标；
- 点击后使用完整会话 ID 跳转到 Codex，不展示会话 ID；
- 胶囊过多时手动横向滑动，滑动位置在普通状态刷新时保持。

### 8.3 明确不做

- 不显示 `1/2`、`2/2`；
- 不每隔数秒自动切换任务；
- 不在完整面板加入常驻左右箭头；
- 不展示提示词、文件名或会话 ID；
- 不让内部 Subagent 增加任务数量。

具体任务查看留在 Codex 主应用；Touch Bar 只负责紧凑导航，菜单栏诊断只提供匿名数量和状态分类。

## 9. 事件反馈

持续状态和瞬时事件必须分开。

| 事件 | 反馈 | 生命周期 |
|---|---|---|
| 工作开始 | 宠物进入工作姿态 | 持续到状态改变 |
| 任务完成 | 绿色 + 一次短庆祝 | 展示一次后回到当前持续状态 |
| 任务失败 | 一次失败提示 | 不循环，不覆盖其他正在工作任务 |
| 等待用户 | 琥珀色进入提示 | 首次提示后保持静止 |
| 连接断开 | 灰色重连状态 | 恢复或进入过期状态 |
| 连接恢复 | 一次轻微恢复反馈 | 不重放旧事件 |
| 当前系统异常 | 红色警示 | 异常解除前保持，但不闪烁 |

事件需要唯一标识或消费记录，避免应用重连、重新订阅或快照重放时重复庆祝。

## 10. 交互设计

### 10.1 Touch Bar

- 点击紧凑宠物：激活 Codex，并展开完整状态；
- Codex 成为前台：自动展开，除非开启安静模式；
- 切到其他应用：收起完整面板，保留紧凑宠物；
- 点击完整状态：只激活 Codex，不执行审批或任务操作；
- 系统控制条需要空间时：宠物必须让位并能恢复；
- 动画进行中收到新状态：立即从当前画面过渡，不等待旧动画结束。

首版不使用长按、双击和隐藏手势。安静模式等低频操作放在菜单栏，避免不可发现的交互。

### 10.2 自动展开策略

自动展开只由明确情境触发：

- Codex 成为前台；
- 用户主动点击紧凑宠物；
- 用户在菜单栏选择“在 Touch Bar 展开”。

任务完成和普通后台工作不能抢占其他应用的完整 Touch Bar。等待和严重错误可以改变窄宠物状态，但默认不强制展开。

## 11. 动效规范

- 所有状态切换都可被下一状态立即打断；
- 普通过渡使用 120 至 180ms 交叉淡变；
- 展开和收起沿同一路径变化，不从无关方向飞入；
- 空闲眨眼每 4 至 8 秒最多一次；
- 工作循环保持低幅度，避免高频位移；
- 等待和错误只在进入状态时提示一次，随后静止；
- 完成动画总时长不超过 1.2 秒，只播放一次；
- 减少动态开启时取消弹性、位移和循环动作，只保留短淡变；
- 不使用快速闪烁和突变亮度。

动效的目标是让状态变化可感知，不是证明宠物一直“活着”。

## 12. 菜单栏设置

首版设置控制在五项以内：

1. Codex 前台时自动展开；
2. 保留 Touch Bar 紧凑宠物；
3. 安静模式；
4. 动效强度：跟随系统 / 标准 / 静态；
5. 完成与等待提示开关。

后续可以增加主题选择，但不能在没有正式宠物资产前提供空主题入口。

## 13. 诊断设计

诊断面板用于解释“为什么没显示”或“为什么显示异常”，不面向日常浏览。

应包含：

- Codex 进程状态；
- IPC 连接状态；
- 最后一次成功更新时间；
- 顶层任务、等待任务和被过滤内部任务数量；
- Touch Bar 私有能力探测结果；
- 当前降级模式；
- 最近一次断线或解析错误的时间与错误码；
- “重新连接”和“复制诊断摘要”。

诊断摘要不能包含对话正文、任务标题、命令、文件路径或令牌。

## 14. 隐私与安全

- 状态适配器入口只保留枚举、计数、时间和匿名线程关系；
- 不持久化提示词、回复正文、附件、命令、文件名和工具参数；
- 默认不记录逐事件日志；
- 诊断日志采用短期、可清理和无正文格式；
- 不需要 OpenAI API Key，不向第三方服务发送数据；
- 不修改 SIP，不注入 Codex 或系统进程，不自动重启系统服务；
- Touch Bar 不承载审批和破坏性操作。

## 15. 兼容与降级

```text
完整兼容
  -> Touch Bar 紧凑态 + 完整态 + 菜单栏

Touch Bar 私有接口不可用
  -> 仅菜单栏状态与诊断

Codex IPC 不兼容或断开
  -> 宠物显示未连接/状态过期，菜单栏给出原因

Codex 未运行
  -> 紧凑宠物休息，点击可启动 Codex
```

应用退出、崩溃或被终止后必须释放自身 Touch Bar 项，不影响系统 Control Strip。

## 16. 非功能目标

- 状态可辨识时间：用户一眼即可分辨工作、等待、完成和异常；
- 状态更新目标：正常连接时尽量在 1 秒内反映主要状态变化；
- 长时间稳定性：实体 Touch Bar 连续运行 2 小时，不丢失紧凑入口、不阻塞系统控制；
- 恢复能力：Codex 重启、IPC 断线和应用切换后可以自行恢复；
- 资源控制：空闲时不做高频动画、轮询或日志写入；
- 隐私：默认运行不产生包含用户对话内容的本地文件。

## 17. 版本路线

### v0.2：可日用状态伴侣

- 三维状态模型；
- 顶层任务与内部 Subagent 分层；
- 历史错误和过期状态处理；
- 稳定的紧凑态、完整态和可手动滑动的多任务导航；
- 菜单栏基础设置与诊断；
- 安静模式和减少动态；
- 一次性事件去重。

### v0.3：正式宠物视觉（进行中）

- 原创狐狸素材已确认，全部语义状态的 6 帧候选动画已接入测试构建；
- 完成实体 Touch Bar 上的连贯性、节奏和状态辨识验收；
- 主题能力；
- 动效和 OLED 长时显示优化；
- 无障碍与高对比模式完善。

### v1.0：可分发版本

- Developer ID 签名与 notarization；
- 首次启动和兼容性自检；
- 崩溃恢复与可导出的隐私安全诊断包；
- 无 Touch Bar 的菜单栏完整回退；
- 升级兼容策略和版本说明。

### 后续研究

- 当前打开会话的可靠识别；
- 用户主动查看后台任务；
- 可选的轻量工作统计。

研究项只有在状态来源可被证明可靠后才能进入正式版本范围。

## 18. v0.2 验收标准

1. 其他应用前台时，紧凑宠物持续存在；Codex 前台时完整面板自动出现。
2. 工作状态稳定显示蓝色“工作中”，不会长期显示工具调用。
3. 多任务以状态胶囊并排显示，可手动横向滑动；不出现分页编号、自动轮播和无意义左右切换。
4. 内部 Subagent 不计入顶层任务数量，也不能把历史错误提升成全局系统异常。
5. 红色只用于当前有效严重错误；历史失败保留在诊断中。
6. 等待确认和等待回复能被明确区分，并在处理后恢复为工作或空闲。
7. IPC 断开后先显示重连，再在超时后显示状态过期，不能无限保留旧状态。
8. 完成、失败和恢复事件只提示一次，重连或快照重放不会重复动画。
9. 开启减少动态后，不再出现循环位移、弹性和闪烁。
10. 应用退出后系统 Control Strip 正常恢复；无 Touch Bar 或接口不兼容时菜单栏仍可用。
11. 默认日志和诊断摘要不包含对话正文、命令、文件名、附件路径或工具参数。
12. 任务胶囊使用完整会话 ID 精确跳转；不使用标题或最近更新时间伪造当前会话高亮。

## 19. 已确认决策与视觉测试门禁

以下三项已经确认：

1. 正式视觉方向采用“原创小型像素宠物”，但必须先通过候选测试；
2. 红色只表示当前有效的严重错误；
3. Touch Bar 只显示紧凑任务胶囊并提供精确跳转，具体任务内容仍回到 Codex 查看。

候选测试采用三轮方案：

1. 静态识别测试：三种候选、九种状态、盲测识别和非颜色识别；
2. 真实 Touch Bar 测试：保留第一轮前两名，检查窄入口、完整汇总、状态切换、控制条让位和减少动态；
3. 长时间低干扰测试：对前两名进行 30–60 分钟实际观察。

测试材料位于 `design-test/`，与正式应用资源和 `Sources/` 隔离。只有用户确认测试结果，且三轮门槛全部通过，才允许进入正式美术需求和技术实现。

指定会话的精确跳转已纳入实现；当前打开会话的持续识别仍然延期，也不能用最近更新时间进行伪判断。

其余实现参数，包括新鲜度时间窗口、动画帧率和像素画布尺寸，应在技术设计与真机验证阶段确定，不应由产品文档提前虚构。
